import { StyleSheet } from "react-native";

export function getInlineLoadingStyles() {
  return StyleSheet.create({
    wrap: {
      alignItems: "center",
      marginTop: 16,
      paddingVertical: 24,
    },
  });
}
