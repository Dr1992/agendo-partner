import { StyleSheet } from "react-native";

import type { AppTheme } from "../../../theme";

export function getDisponibilidadeListStyles(_theme: AppTheme) {
  return StyleSheet.create({
    gateText: {
      textAlign: "center",
    },
  });
}
